# PDF2Muse 图标 v5

![图标 v5](app-icon-v5.png)

- 恢复 v3 中 `p` 与 `m` 的原始字形、笔画粗细和圆润比例。
- 只对字形做等比例缩放和位置调整，没有重新设计字形结构。
- `m` 整体移动到右下，与 `p` 断开。
- 紫色箭头紧邻 `p` 的右上侧。
- 仍保持左上、左下、右下为主体，右上保留适量空间。

SVG 是可编辑源文件，PNG 是 1024 × 1024 预览。
