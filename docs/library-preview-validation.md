# 曲谱库预览验证（2026-09-18）

## 显示回归
Qt PDF 渲染结果可能含透明纸张。直接显示在深色 QLabel 上会导致黑色音符几乎不可见。现在在后台将渲染结果合成到不透明白色 QImage，再交给界面显示。新增真实矢量 PDF 测试检查白纸像素及 alpha。

## 连续切换
读取本机曲谱库的 5 份实际 PDF（4、19、2、11、6 页），不修改曲谱库或 PDF。预热每份首面后，连续切换 400 次、每次间隔 20 ms。

- 所有 5 份封面成功加载。
- 总渲染次数 5；重复选择命中缓存。
- 切换前后进程内存增长 0.68 MiB。
- 全过程 10 ms 界面计时器最大间隔 176.9 ms；未出现持续无响应。
- 缓存上限 12 张；新选择清除尚未开始的旧任务，正在执行的旧结果不会覆盖当前选择。
- PDF 文档只在单一后台线程创建、渲染和关闭，界面不再共享可被反复关闭的文档。

以上为当前机器源码版本的测试结果，不能证明旧版本卡顿的唯一原因，也不覆盖所有 PDF 或失联磁盘。全部 20 项单元测试通过。
